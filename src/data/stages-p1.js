export const stagesPart1 = [
  // ─────────────────────────────────────────────
  // REGION 1: Ground School — Python Basics
  // ─────────────────────────────────────────────
  {
    id: 1,
    regionId: 1,
    regionStage: 1,
    title: 'Engine Start',
    subtitle: 'print() basics',
    briefing: 'Captain, all pre-flight checks are complete. Your first task: establish comms with Singapore Tower. In Python, we use the print() function to send output.',
    concept: 'print()',
    lesson: 'The `print()` function displays text to the screen. Text (called a string) must be wrapped in quotes:\n\n`print("Hello, World!")`\n\nYou can print numbers without quotes:\n\n`print(42)`\n\nYou can print multiple items separated by commas:\n\n`print("Altitude:", 35000)`',
    starterCode: '# Establish comms with Singapore Tower\n# Print exactly: Hello, Tower!\n\n',
    solution: 'print("Hello, Tower!")\n',
    tests: [
      { description: 'Tower comms established', expectedOutput: 'Hello, Tower!' },
    ],
    hint: 'Use print("Hello, Tower!") — the text must be inside quotes.',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 2,
    regionId: 1,
    regionStage: 2,
    title: 'Pre-flight Variables',
    subtitle: 'Variables & assignment',
    briefing: 'Before pushback, you store all flight parameters in the aircraft computer. In Python, variables are named containers that store values for later use.',
    concept: 'variables, =',
    lesson: 'A variable stores a value using the = sign:\n\n`altitude = 35000`\n\nYou can store text (strings), numbers (integers or floats), and more:\n\n`callsign = "SQ321"\nspeed = 480\nfuel_kg = 45200.5`\n\nPrint a variable directly:\n\n`print(callsign)`',
    starterCode: '# Store your flight parameters\nflight_number = ___\norigin = ___\ndestination = ___\n\n# Print them\nprint(flight_number)\nprint(origin)\nprint(destination)\n',
    solution: 'flight_number = "SQ321"\norigin = "SIN"\ndestination = "LHR"\n\nprint(flight_number)\nprint(origin)\nprint(destination)\n',
    tests: [
      { description: 'Flight number stored', expectedOutput: 'SQ321' },
      { description: 'Airports stored', expectedOutput: 'SIN' },
    ],
    hint: 'Replace ___ with values in quotes for text, e.g. flight_number = "SQ321"',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 3,
    regionId: 1,
    regionStage: 3,
    title: 'Aircraft Types',
    subtitle: 'Data types',
    briefing: "The aircraft's weight and balance computer needs to know what type of data each value is — whether it's a whole number, a decimal, text, or a yes/no value.",
    concept: 'int, float, str, bool, type()',
    lesson: 'Python has four basic types:\n- `int`: whole numbers — `altitude = 35000`\n- `float`: decimals — `fuel_tons = 45.2`\n- `str`: text — `callsign = "SQ321"`\n- `bool`: True/False — `gear_up = True`\n\nCheck a type with `type()`:\n\n`print(type(35000))` → `<class \'int\'>`',
    starterCode: '# Declare these variables with correct types\naltitude = ___        # whole number: 35000\nfuel_tons = ___       # decimal: 45.2\ncallsign = ___        # text: "SQ321"\ngear_up = ___         # boolean: True\n\nprint(type(altitude))\nprint(type(fuel_tons))\nprint(type(callsign))\nprint(type(gear_up))\n',
    solution: 'altitude = 35000\nfuel_tons = 45.2\ncallsign = "SQ321"\ngear_up = True\n\nprint(type(altitude))\nprint(type(fuel_tons))\nprint(type(callsign))\nprint(type(gear_up))\n',
    tests: [
      { description: 'int detected', expectedOutput: "<class 'int'>" },
      { description: 'float detected', expectedOutput: "<class 'float'>" },
      { description: 'str detected', expectedOutput: "<class 'str'>" },
      { description: 'bool detected', expectedOutput: "<class 'bool'>" },
    ],
    hint: 'altitude should be 35000 (no quotes), fuel_tons should be 45.2, callsign should be "SQ321" (quotes), gear_up should be True (capital T)',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 4,
    regionId: 1,
    regionStage: 4,
    title: 'Callsign Builder',
    subtitle: 'String concatenation & f-strings',
    briefing: 'ATC is requesting your full callsign. You need to build it by combining your airline code and flight number. Python gives you two ways to join strings.',
    concept: 'string concatenation, f-strings',
    lesson: 'Join strings with +:\n\n`airline = "Singapore"\nflight = "321"\nprint(airline + " Airlines Flight " + flight)`\n\nOr use f-strings (cleaner):\n\n`callsign = f"{airline} Airlines Flight {flight}"\nprint(callsign)`\n\nF-strings use curly braces {} to embed variables directly.',
    starterCode: 'airline_code = "SQ"\nflight_num = "321"\nairport = "Changi"\n\n# Build full callsign using f-string\ncallsign = f"___ Airlines, Flight ___"\nprint(callsign)\n\n# Build arrival message\narrival = f"Arriving ___, 07:30 local"\nprint(arrival)\n',
    solution: 'airline_code = "SQ"\nflight_num = "321"\nairport = "Changi"\n\ncallsign = f"SQ Airlines, Flight {flight_num}"\nprint(callsign)\n\narrival = f"Arriving {airport}, 07:30 local"\nprint(arrival)\n',
    tests: [
      { description: 'Callsign built', expectedOutput: 'SQ Airlines, Flight 321' },
      { description: 'Arrival message', expectedOutput: 'Arriving Changi' },
    ],
    hint: 'Use {variable_name} inside the f-string: f"SQ Airlines, Flight {flight_num}"',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 5,
    regionId: 1,
    regionStage: 5,
    title: 'Weight & Balance',
    subtitle: 'Arithmetic operators',
    briefing: 'Weight and balance calculations are critical before every flight. The load team has sent the numbers — now you need to compute the final values.',
    concept: '+, -, *, /, //, %, **',
    lesson: 'Python arithmetic operators:\n- `+` add, `-` subtract, `*` multiply, `/` divide (gives float)\n- `//` floor division (integer result), `%` modulo (remainder), `**` power\n\n`total = 45200 + 18500   # 63700`\n`half = 100 / 2          # 50.0`\n`remainder = 17 % 5     # 2`\n`squared = 4 ** 2        # 16`',
    starterCode: 'payload_kg = 18500\nfuel_kg = 45200\nzfw_kg = 145000  # zero fuel weight\n\n# Calculate takeoff weight\ntow = ___\n\n# Calculate fuel in tonnes (1 tonne = 1000 kg)\nfuel_tonnes = ___\n\n# Fuel per hour if burn rate is 6800 kg/h — flight is 12.5 hours\ntotal_burn = ___\n\nprint(f"TOW: {tow} kg")\nprint(f"Fuel: {fuel_tonnes} tonnes")\nprint(f"Total burn: {total_burn} kg")\n',
    solution: 'payload_kg = 18500\nfuel_kg = 45200\nzfw_kg = 145000\n\ntow = zfw_kg + fuel_kg\nfuel_tonnes = fuel_kg / 1000\ntotal_burn = 6800 * 12.5\n\nprint(f"TOW: {tow} kg")\nprint(f"Fuel: {fuel_tonnes} tonnes")\nprint(f"Total burn: {total_burn} kg")\n',
    tests: [
      { description: 'TOW calculated', expectedOutput: 'TOW: 190200 kg' },
      { description: 'Fuel in tonnes', expectedOutput: 'Fuel: 45.2 tonnes' },
      { description: 'Burn calculated', expectedOutput: 'Total burn: 85000.0 kg' },
    ],
    hint: 'TOW = zfw_kg + fuel_kg, fuel_tonnes = fuel_kg / 1000, total_burn = 6800 * 12.5',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 6,
    regionId: 1,
    regionStage: 6,
    title: 'Fuel Planner',
    subtitle: 'Operator precedence & rounding',
    briefing: 'The fuel planner wants a precise calculation. Raw arithmetic often produces long decimals — round() keeps the output clean. Operator precedence (BODMAS) controls calculation order.',
    concept: 'operator precedence, round(), abs()',
    lesson: 'Python follows BODMAS order. Use parentheses to control it:\n\n`result = 2 + 3 * 4    # 14, not 20`\n`result = (2 + 3) * 4  # 20`\n\nRound with round():\n\n`round(3.14159, 2)  # 3.14`\n\nAbsolute value:\n\n`abs(-350)  # 350`',
    starterCode: 'distance_nm = 3365    # Singapore to London\nspeed_kts = 480       # cruise speed\nfuel_flow_kh = 6800   # kg per hour\n\n# Flight time in hours (distance / speed)\nflight_time = ___\n\n# Fuel required\nfuel_req = ___\n\n# Reserve fuel (10% of required)\nreserve = ___\n\n# Total fuel, rounded to nearest 10 kg\ntotal_fuel = ___\n\nprint(f"Flight time: {round(flight_time, 1)} hours")\nprint(f"Fuel required: {fuel_req} kg")\nprint(f"Reserve: {reserve} kg")\nprint(f"Total: {total_fuel} kg")\n',
    solution: 'distance_nm = 3365\nspeed_kts = 480\nfuel_flow_kh = 6800\n\nflight_time = distance_nm / speed_kts\nfuel_req = fuel_flow_kh * flight_time\nreserve = fuel_req * 0.1\ntotal_fuel = round(fuel_req + reserve, -1)\n\nprint(f"Flight time: {round(flight_time, 1)} hours")\nprint(f"Fuel required: {fuel_req} kg")\nprint(f"Reserve: {reserve} kg")\nprint(f"Total: {total_fuel} kg")\n',
    tests: [
      { description: 'Flight time shown', expectedOutput: 'Flight time: 7.0 hours' },
      { description: 'Total fuel rounded', expectedOutput: 'Total:' },
    ],
    hint: 'flight_time = distance_nm / speed_kts. Then fuel_req = fuel_flow_kh * flight_time',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 7,
    regionId: 1,
    regionStage: 7,
    title: 'ATIS Decode',
    subtitle: 'String methods',
    briefing: 'The ATIS broadcast comes in mixed case with extra spaces. You need to clean it up and standardize it before displaying on the cockpit screen.',
    concept: 'str.upper(), str.lower(), str.strip(), str.replace(), len()',
    lesson: 'Common string methods:\n\n`s = "  Singapore  "\ns.strip()         # "Singapore" — removes spaces\ns.upper()         # "  SINGAPORE  "\ns.lower()         # "  singapore  "\n\ns = "SIN-LHR"\ns.replace("-", " to ")  # "SIN to LHR"\n\nlen("SQ321")  # 5`',
    starterCode: 'raw_atis = "  information BRAVO. wind 200 at 12.  visibility 10km.  "\n\n# Clean up the ATIS\ncleaned = raw_atis.___()        # remove leading/trailing spaces\nuppercased = cleaned.___()      # all uppercase\n\n# Count characters\nchar_count = ___(uppercased)\n\nprint(uppercased)\nprint(f"Length: {char_count} characters")\n',
    solution: 'raw_atis = "  information BRAVO. wind 200 at 12.  visibility 10km.  "\n\ncleaned = raw_atis.strip()\nuppercased = cleaned.upper()\nchar_count = len(uppercased)\n\nprint(uppercased)\nprint(f"Length: {char_count} characters")\n',
    tests: [
      { description: 'ATIS uppercased', expectedOutput: 'INFORMATION BRAVO' },
      { description: 'Length shown', expectedOutput: 'Length:' },
    ],
    hint: 'Use .strip() to remove spaces, .upper() to capitalize, len() to count',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 8,
    regionId: 1,
    regionStage: 8,
    title: 'Multi-line Briefing',
    subtitle: 'Multiline strings & string indexing',
    briefing: 'The dispatch team sends multi-line flight briefings. You need to extract specific parts — the aircraft type from the first line and the destination code from the route string.',
    concept: 'triple quotes, string indexing, slicing',
    lesson: 'Triple quotes allow multi-line strings:\n\n`briefing = """Line 1\nLine 2\nLine 3"""`\n\nAccess characters by index (0-based):\n\n`s = "SQ321"\ns[0]     # "S"\ns[-1]    # "1" (last character)\ns[2:5]   # "321" (slice)`',
    starterCode: 'briefing = """Aircraft: A350-900\nRoute: SIN-LHR\nFlight: SQ321\nETD: 23:55 SGT"""\n\n# Print the whole briefing\nprint(briefing)\n\n# Extract the 5th character (index 4) from the briefing\nfifth_char = briefing[___]\nprint(f"5th character: {fifth_char}")\n\n# Extract first 2 characters\nfirst_two = briefing[___]\nprint(f"First two: {first_two}")\n',
    solution: 'briefing = """Aircraft: A350-900\nRoute: SIN-LHR\nFlight: SQ321\nETD: 23:55 SGT"""\n\nprint(briefing)\n\nfifth_char = briefing[4]\nprint(f"5th character: {fifth_char}")\n\nfirst_two = briefing[:2]\nprint(f"First two: {first_two}")\n',
    tests: [
      { description: 'Briefing printed', expectedOutput: 'Aircraft: A350-900' },
      { description: '5th character found', expectedOutput: '5th character:' },
    ],
    hint: 'Index 4 is the 5th character (Python counts from 0). Slice [:2] gets first 2 characters.',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 9,
    regionId: 1,
    regionStage: 9,
    title: 'Flight Log Comments',
    subtitle: 'Code style & comments',
    briefing: 'A well-documented flight plan is a safe flight plan. Comments in Python explain what your code does — the computer ignores them, but your future self (and co-pilot) will thank you.',
    concept: '# comments, code readability',
    lesson: 'Comments start with #:\n\n`# This is a comment\naltitude = 35000  # feet above sea level`\n\nGood comments explain WHY, not just WHAT:\n\n`# ICAO requires 30min reserve for international flights\nreserve_min = 30`\n\nUse comments to disable code temporarily:\n\n`# print("debug info")  # disabled for production`',
    starterCode: '# ============================================\n# FLIGHT PLAN: SIN to LHR\n# ============================================\n\n___ Calculate cruise altitude\naltitude_ft = 35000\n\n___ Convert to meters (1 ft = 0.3048 m)\naltitude_m = altitude_ft * 0.3048\n\n___ Print the result\nprint(f"Cruise altitude: {altitude_ft} ft / {round(altitude_m)} m")\n',
    solution: '# ============================================\n# FLIGHT PLAN: SIN to LHR\n# ============================================\n\n# Calculate cruise altitude\naltitude_ft = 35000\n\n# Convert to meters (1 ft = 0.3048 m)\naltitude_m = altitude_ft * 0.3048\n\n# Print the result\nprint(f"Cruise altitude: {altitude_ft} ft / {round(altitude_m)} m")\n',
    tests: [
      { description: 'Altitude printed', expectedOutput: 'Cruise altitude: 35000 ft' },
    ],
    hint: 'Replace ___ with # (hash symbol) to make them comments',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 10,
    regionId: 1,
    regionStage: 10,
    title: 'First Solo Check',
    subtitle: 'Ground School Milestone',
    briefing: "Captain, you've completed Ground School! This is your first solo check ride. Put everything together: read flight data, calculate performance, format the output. Clear skies ahead.",
    concept: 'Review: print, variables, types, strings, arithmetic, comments',
    lesson: 'Review check:\n\n`name = "SQ321"           # str\nalt = 35000              # int\nspeed = 480.5            # float\nairborne = True          # bool\n\nprint(f"Flight {name}: {alt}ft, {speed}kts")`\n\nArithmetic: + - * / // % **\nStrings: .upper() .lower() .strip() .replace() len()\nSlicing: s[0], s[-1], s[2:5]',
    starterCode: '# SOLO CHECK — Put it all together!\n\n# Flight data\nflight = "SQ317"\npax = 287\ncargo_kg = 15320\nfuel_kg = 48750\nzfw = 152000\n\n# Calculate TOW\ntow = ___\n\n# Calculate payload percentage of TOW\npayload_pct = ___\n\n# Build status message\nstatus = f"Flight {flight}: {pax} pax, TOW {tow}kg, payload {round(payload_pct, 1)}%"\nprint(status.upper())\nprint(f"Fuel: {fuel_kg / 1000} tonnes")\n',
    solution: 'flight = "SQ317"\npax = 287\ncargo_kg = 15320\nfuel_kg = 48750\nzfw = 152000\n\ntow = zfw + fuel_kg\npayload_pct = (cargo_kg / tow) * 100\n\nstatus = f"Flight {flight}: {pax} pax, TOW {tow}kg, payload {round(payload_pct, 1)}%"\nprint(status.upper())\nprint(f"Fuel: {fuel_kg / 1000} tonnes")\n',
    tests: [
      { description: 'Status uppercased', expectedOutput: 'FLIGHT SQ317' },
      { description: 'Fuel in tonnes', expectedOutput: 'Fuel: 48.75 tonnes' },
    ],
    hint: 'tow = zfw + fuel_kg, payload_pct = (cargo_kg / tow) * 100',
    xp: 500,
    isMilestone: true,
  },

  // ─────────────────────────────────────────────
  // REGION 2: Traffic Control — Control Flow
  // ─────────────────────────────────────────────
  {
    id: 11,
    regionId: 2,
    regionStage: 1,
    title: 'Go/No-Go Decision',
    subtitle: 'if/else',
    briefing: 'Before departure, you must make a go/no-go call. Visibility below minimums means NO GO. This is the most fundamental decision in aviation — and in programming.',
    concept: 'if, else, boolean conditions',
    lesson: '`if condition:\n    # runs when True\nelse:\n    # runs when False`\n\n`visibility = 800\nif visibility >= 550:\n    print("GO — above CAT I minimums")\nelse:\n    print("NO GO — below minimums")`',
    starterCode: 'visibility_m = 450\ncrosswind_kts = 28\nmax_crosswind = 35\n\n# Check if go for takeoff\nif ___:\n    print("GO — conditions acceptable")\nelse:\n    print("NO GO — below minimums")\n',
    solution: 'visibility_m = 450\ncrosswind_kts = 28\nmax_crosswind = 35\n\nif visibility_m >= 550:\n    print("GO — conditions acceptable")\nelse:\n    print("NO GO — below minimums")\n',
    tests: [
      { description: 'No-go call made', expectedOutput: 'NO GO — below minimums' },
    ],
    hint: 'visibility_m = 450 is below 550, so the condition should be visibility_m >= 550',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 12,
    regionId: 2,
    regionStage: 2,
    title: 'Runway Selector',
    subtitle: 'Comparison operators',
    briefing: 'Wind direction determines which runway to use. 20R for winds 180-260, 02L otherwise. These decisions use comparison operators — the language of conditions.',
    concept: '==, !=, <, >, <=, >=',
    lesson: 'Comparison operators return True or False:\n\n`5 > 3    # True\n5 == 5   # True (note: == not =)\n5 != 3   # True\n5 >= 5   # True\n5 < 3    # False`\n\nDon\'t confuse = (assignment) with == (comparison).',
    starterCode: 'wind_dir = 220  # degrees\n\n# Use 20R if wind is between 180 and 260 degrees\nif wind_dir ___ 180 and wind_dir ___ 260:\n    print("Use Runway 20R")\nelse:\n    print("Use Runway 02L")\n\n# Check if perfectly on the nose\nif wind_dir ___ 200:\n    print("Perfect headwind on 20R")\n',
    solution: 'wind_dir = 220\n\nif wind_dir >= 180 and wind_dir <= 260:\n    print("Use Runway 20R")\nelse:\n    print("Use Runway 02L")\n\nif wind_dir == 200:\n    print("Perfect headwind on 20R")\n',
    tests: [
      { description: 'Runway selected', expectedOutput: 'Use Runway 20R' },
    ],
    hint: "Use >= for 'greater than or equal' and <= for 'less than or equal'",
    xp: 100,
    isMilestone: false,
  },
  {
    id: 13,
    regionId: 2,
    regionStage: 3,
    title: 'Clearance Levels',
    subtitle: 'elif chain',
    briefing: 'ATC issues clearances in priority tiers. You need to categorize each clearance type. When there are more than 2 options, elif chains handle multiple conditions cleanly.',
    concept: 'elif',
    lesson: '`if condition1:\n    # ...\nelif condition2:\n    # ...\nelif condition3:\n    # ...\nelse:\n    # default`\n\nPython checks each condition top-to-bottom and stops at the first True.',
    starterCode: 'flight_level = 390\n\n# Categorize altitude\nif flight_level >= 410:\n    print("Upper airspace — RVSM +")\nelif flight_level ___ 290:\n    print("RVSM airspace — FL290-FL410")\nelif flight_level ___ 180:\n    print("Transition — FL180-FL290")\n___:\n    print("Low level — below FL180")\n',
    solution: 'flight_level = 390\n\nif flight_level >= 410:\n    print("Upper airspace — RVSM +")\nelif flight_level >= 290:\n    print("RVSM airspace — FL290-FL410")\nelif flight_level >= 180:\n    print("Transition — FL180-FL290")\nelse:\n    print("Low level — below FL180")\n',
    tests: [
      { description: 'RVSM airspace identified', expectedOutput: 'RVSM airspace' },
    ],
    hint: "elif goes between if and else. Don't forget else: at the end.",
    xp: 100,
    isMilestone: false,
  },
  {
    id: 14,
    regionId: 2,
    regionStage: 4,
    title: 'Weather Minimums',
    subtitle: 'Boolean operators (and, or, not)',
    briefing: 'The dispatch team sends multiple weather parameters. You need to combine them into a single go/no-go decision. Boolean operators let you combine multiple conditions.',
    concept: 'and, or, not',
    lesson: '`and` — both must be True: `a and b`\n`or` — at least one True: `a or b`\n`not` — flips True/False: `not a`\n\n`vis = 800\nwind = 15\nraining = True\n\nif vis >= 550 and wind <= 35 and not raining:\n    print("VFR conditions")`',
    starterCode: 'visibility = 1200\nceiling_ft = 800\ncrosswind = 18\nthunderstorm = False\n\n# VFR requires: vis >= 5000m, ceiling >= 1000ft\nvfr = visibility ___ 5000 ___ ceiling_ft ___ 1000\nprint(f"VFR: {vfr}")\n\n# CAT I ILS: vis >= 550m, no thunderstorm\ncat1 = visibility ___ 550 ___ ___ thunderstorm\nprint(f"CAT I: {cat1}")\n\n# Can we depart? (CAT I or VFR, crosswind <= 35)\ndepart = (cat1 ___ vfr) ___ crosswind <= 35\nprint(f"Cleared for departure: {depart}")\n',
    solution: 'visibility = 1200\nceiling_ft = 800\ncrosswind = 18\nthunderstorm = False\n\nvfr = visibility >= 5000 and ceiling_ft >= 1000\nprint(f"VFR: {vfr}")\n\ncat1 = visibility >= 550 and not thunderstorm\nprint(f"CAT I: {cat1}")\n\ndepart = (cat1 or vfr) and crosswind <= 35\nprint(f"Cleared for departure: {depart}")\n',
    tests: [
      { description: 'VFR status shown', expectedOutput: 'VFR: False' },
      { description: 'CAT I status shown', expectedOutput: 'CAT I: True' },
      { description: 'Departure cleared', expectedOutput: 'Cleared for departure: True' },
    ],
    hint: 'VFR needs both visibility AND ceiling. CAT I needs visibility AND not thunderstorm.',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 15,
    regionId: 2,
    regionStage: 5,
    title: 'Altitude Bands',
    subtitle: 'Nested if',
    briefing: 'The flight management system categorizes altitude in tiers for different rules and reporting. Inside an if block, you can have another if block — this is called nesting.',
    concept: 'nested if/else',
    lesson: 'Nested ifs go inside the body of another if:\n\n`if altitude > 0:\n    if altitude > 10000:\n        print("High altitude")\n    else:\n        print("Low altitude")\nelse:\n    print("On ground")`\n\nUse sparingly — more than 3 levels gets hard to read.',
    starterCode: 'altitude = 28000\nweight_tons = 155\n\n# First: is aircraft airborne?\nif altitude > 0:\n    # Nested: what altitude band?\n    if altitude >= 29000:\n        print("RVSM — 1000ft separation")\n        if weight_tons > 136:\n            print("Heavy — wake turbulence category H")\n    elif altitude >= 10000:\n        print("High altitude — speed unrestricted")\n    ___:\n        print("Below 10,000ft — 250kt limit")\nelse:\n    print("On ground")\n',
    solution: 'altitude = 28000\nweight_tons = 155\n\nif altitude > 0:\n    if altitude >= 29000:\n        print("RVSM — 1000ft separation")\n        if weight_tons > 136:\n            print("Heavy — wake turbulence category H")\n    elif altitude >= 10000:\n        print("High altitude — speed unrestricted")\n    else:\n        print("Below 10,000ft — 250kt limit")\nelse:\n    print("On ground")\n',
    tests: [
      { description: 'Altitude band identified', expectedOutput: 'High altitude — speed unrestricted' },
    ],
    hint: 'Use else: to catch the remaining case (altitude >= 0 but < 10000)',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 16,
    regionId: 2,
    regionStage: 6,
    title: 'Emergency Protocol',
    subtitle: 'Conditional expressions (ternary)',
    briefing: "In an emergency you need fast, one-line decisions. Python's conditional expression lets you write an if/else on a single line — perfect for quick status checks.",
    concept: 'x if condition else y (ternary)',
    lesson: 'One-line if/else:\n\n`value = A if condition else B`\n\n`status = "GO" if visibility >= 550 else "NO GO"\nprint(status)  # "GO" or "NO GO"`\n\nCan be used anywhere an expression is needed, including inside f-strings:\n\n`print(f"Status: {\'ARMED\' if gear_up else \'DOWN\'}")`',
    starterCode: 'engine1_n1 = 88\nengine2_n1 = 76  # below minimum 80%\n\n# One-line status for each engine\neng1_status = "NORMAL" ___ engine1_n1 >= 80 ___ "LOW POWER"\neng2_status = ___ engine2_n1 >= 80 ___ "NORMAL" ___ "LOW POWER"\n\nprint(f"Engine 1: {eng1_status}")\nprint(f"Engine 2: {eng2_status}")\n\n# Overall status\nall_ok = engine1_n1 >= 80 and engine2_n1 >= 80\nprint(f"All engines: {\'NORMAL\' if all_ok else \'EMERGENCY\'}")\n',
    solution: 'engine1_n1 = 88\nengine2_n1 = 76\n\neng1_status = "NORMAL" if engine1_n1 >= 80 else "LOW POWER"\neng2_status = "NORMAL" if engine2_n1 >= 80 else "LOW POWER"\n\nprint(f"Engine 1: {eng1_status}")\nprint(f"Engine 2: {eng2_status}")\n\nall_ok = engine1_n1 >= 80 and engine2_n1 >= 80\nprint(f"All engines: {\'NORMAL\' if all_ok else \'EMERGENCY\'}")\n',
    tests: [
      { description: 'Engine 1 normal', expectedOutput: 'Engine 1: NORMAL' },
      { description: 'Engine 2 low', expectedOutput: 'Engine 2: LOW POWER' },
      { description: 'Emergency declared', expectedOutput: 'All engines: EMERGENCY' },
    ],
    hint: 'Format: value = A if condition else B — true result comes first, then else, then false result',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 17,
    regionId: 2,
    regionStage: 7,
    title: 'Input Validation',
    subtitle: 'int(), float(), str() conversion',
    briefing: 'The dispatcher sends weight in metric (kg) but the FMS needs pounds. And sometimes data comes in as text that needs to be converted to numbers before you can calculate.',
    concept: 'int(), float(), str(), type conversion',
    lesson: 'Convert between types:\n\n`int("35000")    # 35000 (str → int)`\n`float("45.2")   # 45.2 (str → float)`\n`str(35000)      # "35000" (int → str)`\n\nWarning: int("45.2") raises ValueError — can\'t convert decimal string to int directly.\n\n`int(float("45.2"))  # 45 — convert to float first`',
    starterCode: '# Data arrives as strings from the dispatcher system\nweight_str = "68500"    # kg as string\nfuel_str = "45.8"       # tonnes as string\nflight_str = "321"      # flight number as string\n\n# Convert to usable types\nweight_kg = ___(weight_str)\nfuel_tonnes = ___(fuel_str)\nflight_num = ___(flight_str)  # keep as int for arithmetic\n\n# Convert weight to pounds (1 kg = 2.20462 lbs)\nweight_lbs = weight_kg * 2.20462\n\nprint(f"Weight: {weight_kg} kg / {round(weight_lbs)} lbs")\nprint(f"Fuel: {fuel_tonnes} tonnes")\nprint(f"Flight SQ{flight_num + 1}")  # next flight number\n',
    solution: 'weight_str = "68500"\nfuel_str = "45.8"\nflight_str = "321"\n\nweight_kg = int(weight_str)\nfuel_tonnes = float(fuel_str)\nflight_num = int(flight_str)\n\nweight_lbs = weight_kg * 2.20462\n\nprint(f"Weight: {weight_kg} kg / {round(weight_lbs)} lbs")\nprint(f"Fuel: {fuel_tonnes} tonnes")\nprint(f"Flight SQ{flight_num + 1}")\n',
    tests: [
      { description: 'Weight converted', expectedOutput: 'Weight: 68500 kg' },
      { description: 'Next flight shown', expectedOutput: 'Flight SQ322' },
    ],
    hint: 'Use int() for whole numbers, float() for decimals',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 18,
    regionId: 2,
    regionStage: 8,
    title: 'NOTAM Parser',
    subtitle: 'str.split() and str.join()',
    briefing: 'NOTAMs come as space-separated strings. You need to split them into individual words, check for keywords, and reassemble them. String splitting is one of the most common real-world Python tasks.',
    concept: 'str.split(), str.join(), in operator',
    lesson: '`s = "SIN LHR 23:55 07:40"\nparts = s.split()       # [\'SIN\', \'LHR\', \'23:55\', \'07:40\']\n\n# Split by specific character\nroute = "SIN-DXB-LHR"\nlegs = route.split("-")  # [\'SIN\', \'DXB\', \'LHR\']\n\n# Join a list back into a string\n" → ".join(legs)  # \'SIN → DXB → LHR\'\n\n# Check if word is in string\n"SIN" in route  # True`',
    starterCode: 'notam = "runway 20R closed for maintenance from 0200 to 0600 UTC"\n\n# Split into words\nwords = notam.___()\nprint(f"Word count: {len(words)}")\n\n# Check if runway 20R is mentioned\nif "20R" ___ notam:\n    print("NOTAM affects runway 20R")\n\n# Rebuild in uppercase, words joined by |\nformatted = "|".___([w.upper() for w in words])\nprint(formatted)\n',
    solution: 'notam = "runway 20R closed for maintenance from 0200 to 0600 UTC"\n\nwords = notam.split()\nprint(f"Word count: {len(words)}")\n\nif "20R" in notam:\n    print("NOTAM affects runway 20R")\n\nformatted = "|".join([w.upper() for w in words])\nprint(formatted)\n',
    tests: [
      { description: 'Word count shown', expectedOutput: 'Word count: 10' },
      { description: 'Runway identified', expectedOutput: 'NOTAM affects runway 20R' },
    ],
    hint: 'Use .split() with no arguments to split by spaces. Use \'word\' in string to check membership.',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 19,
    regionId: 2,
    regionStage: 9,
    title: 'Squawk Assignment',
    subtitle: 'Integer operations',
    briefing: 'Squawk codes are 4-digit octal numbers (0-7 each digit). Given a linear code number, you need to convert it to individual digits using integer arithmetic — a classic programming pattern.',
    concept: 'integer division, modulo, divmod()',
    lesson: '`//` floor division gives the quotient:\n`17 // 5  # 3`\n\n`%` modulo gives the remainder:\n`17 % 5   # 2`\n\nTogether they decompose numbers:\n`divmod(17, 5)  # (3, 2) — both at once`\n\nPattern to extract digits:\n`n = 1234\nhundreds = n // 100      # 12\ntens = (n % 100) // 10   # 3\nones = n % 10            # 4`',
    starterCode: 'squawk_code = 2573\n\n# Extract each digit\nthousands = squawk_code ___ 1000\nhundreds = (squawk_code ___ 1000) ___ 100\ntens = (squawk_code ___ 100) ___ 10\nunits = squawk_code ___ 10\n\nprint(f"Squawk: {thousands}-{hundreds}-{tens}-{units}")\nprint(f"Digits sum: {thousands + hundreds + tens + units}")\n\n# Is it a special code?\nif squawk_code == 7700:\n    print("EMERGENCY")\nelif squawk_code == 7500:\n    print("HIJACK")\nelif squawk_code == 7600:\n    print("COMMS FAILURE")\nelse:\n    print("Normal code")\n',
    solution: 'squawk_code = 2573\n\nthousands = squawk_code // 1000\nhundreds = (squawk_code % 1000) // 100\ntens = (squawk_code % 100) // 10\nunits = squawk_code % 10\n\nprint(f"Squawk: {thousands}-{hundreds}-{tens}-{units}")\nprint(f"Digits sum: {thousands + hundreds + tens + units}")\n\nif squawk_code == 7700:\n    print("EMERGENCY")\nelif squawk_code == 7500:\n    print("HIJACK")\nelif squawk_code == 7600:\n    print("COMMS FAILURE")\nelse:\n    print("Normal code")\n',
    tests: [
      { description: 'Squawk digits shown', expectedOutput: 'Squawk: 2-5-7-3' },
      { description: 'Normal code confirmed', expectedOutput: 'Normal code' },
    ],
    hint: 'Use // to get whole quotient, % to get remainder. thousands = squawk_code // 1000',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 20,
    regionId: 2,
    regionStage: 10,
    title: 'IFR Clearance',
    subtitle: 'Traffic Control Milestone',
    briefing: "Captain, you've earned your IFR rating! This check combines all control flow concepts. The dispatch system sends raw data — you must validate, categorize, and format it into a proper clearance.",
    concept: 'Review: if/elif/else, boolean, comparisons, ternary, type conversion',
    lesson: 'Review:\n- `if/elif/else` for branching\n- `and/or/not` for combining conditions\n- `A if cond else B` one-liner\n- `int(), float(), str()` conversions\n- `in` operator for membership\n- Comparison: `==, !=, <, >, <=, >=`',
    starterCode: '# Raw dispatch data (strings)\nalt_str = "35000"\nspd_str = "480"\nvis_str = "800"\nrunway = "20R"\naircrafts = ["A350", "B777", "A380"]\n\n# Convert\nalt = ___(alt_str)\nspd = ___(spd_str)\nvis = ___(vis_str)\n\n# Clearance category\nif vis >= 5000:\n    wx_cat = "VFR"\nelif vis >= 550:\n    wx_cat = "IFR CAT I"\nelif vis >= 300:\n    wx_cat = "IFR CAT II"\nelse:\n    wx_cat = "NO OPERATIONS"\n\n# Build clearance\nclearance = f"Cleared to LHR via route, FL{alt // 100}, {spd}kt, runway {runway}"\nprint(clearance.upper())\nprint(f"Weather: {wx_cat}")\nprint(f"Our aircraft in fleet: {\'A350\' ___ aircrafts}")\n',
    solution: 'alt_str = "35000"\nspd_str = "480"\nvis_str = "800"\nrunway = "20R"\naircrafts = ["A350", "B777", "A380"]\n\nalt = int(alt_str)\nspd = int(spd_str)\nvis = int(vis_str)\n\nif vis >= 5000:\n    wx_cat = "VFR"\nelif vis >= 550:\n    wx_cat = "IFR CAT I"\nelif vis >= 300:\n    wx_cat = "IFR CAT II"\nelse:\n    wx_cat = "NO OPERATIONS"\n\nclearance = f"Cleared to LHR via route, FL{alt // 100}, {spd}kt, runway {runway}"\nprint(clearance.upper())\nprint(f"Weather: {wx_cat}")\nprint(f"Our aircraft in fleet: {\'A350\' in aircrafts}")\n',
    tests: [
      { description: 'Clearance issued', expectedOutput: 'CLEARED TO LHR' },
      { description: 'Weather category', expectedOutput: 'Weather: IFR CAT I' },
      { description: 'Fleet check', expectedOutput: 'Our aircraft in fleet: True' },
    ],
    hint: "Convert alt and spd with int(), vis with int(). Use 'A350' in aircrafts to check membership.",
    xp: 500,
    isMilestone: true,
  },

  // ─────────────────────────────────────────────
  // REGION 3: Autopilot — Loops
  // ─────────────────────────────────────────────
  {
    id: 21,
    regionId: 3,
    regionStage: 1,
    title: 'Preflight Checklist',
    subtitle: 'for loop over list',
    briefing: 'Before every flight, you run through a checklist. In Python, a for loop does the same thing — it goes through each item in a list one by one, automatically.',
    concept: 'for loop, lists, iteration',
    lesson: '`for item in collection:\n    # do something with item`\n\n`checklist = ["fuel", "oil", "tires", "lights"]\nfor item in checklist:\n    print(f"✓ {item} checked")`\n\nThe loop runs once per item, naming each one `item` in turn.',
    starterCode: 'preflight = ["fuel level", "oil pressure", "tire condition", "nav lights", "pitot cover removed", "flaps", "control surfaces"]\n\n# Run through checklist\nfor item in ___:\n    print(f"✓ {item.upper()}")\n\nprint(f"\\nChecklist complete: {len(preflight)} items")\n',
    solution: 'preflight = ["fuel level", "oil pressure", "tire condition", "nav lights", "pitot cover removed", "flaps", "control surfaces"]\n\nfor item in preflight:\n    print(f"✓ {item.upper()}")\n\nprint(f"\\nChecklist complete: {len(preflight)} items")\n',
    tests: [
      { description: 'Items uppercased', expectedOutput: '✓ FUEL LEVEL' },
      { description: 'Count shown', expectedOutput: 'Checklist complete: 7 items' },
    ],
    hint: "Use the list name after 'in': for item in preflight:",
    xp: 100,
    isMilestone: false,
  },
  {
    id: 22,
    regionId: 3,
    regionStage: 2,
    title: 'Altitude Scan',
    subtitle: 'range()',
    briefing: 'During climb, you report passing through every 5,000 feet. Instead of writing each altitude by hand, range() generates a sequence of numbers automatically.',
    concept: 'range(), range(start, stop, step)',
    lesson: '`range(5)           # 0, 1, 2, 3, 4`\n`range(1, 6)        # 1, 2, 3, 4, 5`\n`range(0, 41, 10)   # 0, 10, 20, 30, 40`\n`range(10, 0, -2)   # 10, 8, 6, 4, 2`\n\n`for alt in range(0, 40001, 5000):\n    print(f"Passing FL{alt // 100:03d}")`',
    starterCode: '# Report passing each 5000ft during climb from 0 to 35000\nfor altitude in range(___, ___, ___):\n    print(f"Climbing through {altitude}ft")\n\nprint("Reached cruise FL350")\n\n# Count down during descent from 10000 to 0\nprint("\\nDescending:")\nfor alt in range(10000, ___, -2000):\n    print(f"{alt}ft")\n',
    solution: 'for altitude in range(0, 36000, 5000):\n    print(f"Climbing through {altitude}ft")\n\nprint("Reached cruise FL350")\n\nprint("\\nDescending:")\nfor alt in range(10000, -1, -2000):\n    print(f"{alt}ft")\n',
    tests: [
      { description: 'Climb reported', expectedOutput: 'Climbing through 0ft' },
      { description: 'Cruise reached', expectedOutput: 'Reached cruise FL350' },
      { description: 'Descent started', expectedOutput: 'Descending:' },
    ],
    hint: 'range(start, stop, step). To include 35000, stop must be 36000. For countdown, step is negative.',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 23,
    regionId: 3,
    regionStage: 3,
    title: 'Fuel Monitor',
    subtitle: 'while loop',
    briefing: "The fuel monitor burns through fuel every hour and checks the level. It keeps running as long as there's enough fuel. This is a while loop — it repeats while a condition is True.",
    concept: 'while loop',
    lesson: '`while condition:\n    # runs as long as condition is True`\n\n`fuel = 45000\nwhile fuel > 5000:  # 5000 kg reserve\n    fuel -= 6800    # burn per hour\n    print(f"Fuel remaining: {fuel} kg")`\n\nWARNING: if the condition never becomes False, the loop runs forever. Always ensure it can exit.',
    starterCode: 'fuel_kg = 45200\nburn_rate = 6800  # per hour\nreserve = 5000\nhour = 0\n\nwhile fuel_kg ___ reserve:\n    fuel_kg -= burn_rate\n    hour += 1\n    status = "OK" if fuel_kg > reserve else "LOW"\n    print(f"Hour {hour}: {fuel_kg} kg — {status}")\n\nprint(f"\\nFuel below reserve after {hour} hours")\n',
    solution: 'fuel_kg = 45200\nburn_rate = 6800\nreserve = 5000\nhour = 0\n\nwhile fuel_kg > reserve:\n    fuel_kg -= burn_rate\n    hour += 1\n    status = "OK" if fuel_kg > reserve else "LOW"\n    print(f"Hour {hour}: {fuel_kg} kg — {status}")\n\nprint(f"\\nFuel below reserve after {hour} hours")\n',
    tests: [
      { description: 'Hour 1 shown', expectedOutput: 'Hour 1: 38400 kg' },
      { description: 'Reserve warning', expectedOutput: 'Fuel below reserve' },
    ],
    hint: 'Condition: fuel_kg > reserve (keep flying while fuel is above reserve)',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 24,
    regionId: 3,
    regionStage: 4,
    title: 'Waypoint Log',
    subtitle: 'enumerate()',
    briefing: 'Your route has multiple waypoints and ATC wants the waypoint number plus its name. enumerate() gives you both the index and the value as you loop.',
    concept: 'enumerate()',
    lesson: '`enumerate(iterable)` yields `(index, value)` pairs:\n\n`waypoints = [\'SINAI\', \'BEVOL\', \'BOMBI\']\nfor i, wp in enumerate(waypoints):\n    print(f"{i+1}. {wp}")\n# 1. SINAI\n# 2. BEVOL\n# 3. BOMBI`\n\nStart from a different number: `enumerate(list, start=1)`',
    starterCode: 'route = ["SIN", "SINAI", "BEVOL", "BOMBI", "PASOV", "DXB", "DESDI", "RASVO", "LHR"]\n\n# Print waypoint number and name\nfor ___, ___ in enumerate(route, start=1):\n    marker = "★" if wp in ["SIN", "LHR"] else "◆"\n    print(f"{num:2d}. {marker} {wp}")\n\nprint(f"\\nTotal waypoints: {len(route)}")\n',
    solution: 'route = ["SIN", "SINAI", "BEVOL", "BOMBI", "PASOV", "DXB", "DESDI", "RASVO", "LHR"]\n\nfor num, wp in enumerate(route, start=1):\n    marker = "★" if wp in ["SIN", "LHR"] else "◆"\n    print(f"{num:2d}. {marker} {wp}")\n\nprint(f"\\nTotal waypoints: {len(route)}")\n',
    tests: [
      { description: 'SIN shown as first', expectedOutput: '1. ★ SIN' },
      { description: 'Total count', expectedOutput: 'Total waypoints: 9' },
    ],
    hint: 'Unpack two variables: for num, wp in enumerate(route, start=1)',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 25,
    regionId: 3,
    regionStage: 5,
    title: 'Traffic Hold',
    subtitle: 'break',
    briefing: 'ATC has you scanning traffic. The moment you spot a conflict, you stop scanning and act. break exits a loop immediately — no matter how many iterations are left.',
    concept: 'break',
    lesson: '`for item in list:\n    if condition:\n        break  # exit the loop NOW`\n\n`traffic = [120, 115, 350, 240, 180]\nfor bearing in traffic:\n    if bearing == 350:   # north — conflict\n        print(f"CONFLICT at {bearing}°")\n        break\n    print(f"Clear at {bearing}°")`',
    starterCode: 'contacts = [15, 45, 90, 120, 0, 180, 270]  # 0 = head-on collision course!\n\nfor bearing in contacts:\n    if bearing ___:  # 0 = directly ahead\n        print(f"HEAD-ON TRAFFIC at {bearing}° — BREAK RIGHT")\n        ___\n    print(f"Traffic at {bearing}° — no conflict")\n\nprint("Scan complete")\n',
    solution: 'contacts = [15, 45, 90, 120, 0, 180, 270]\n\nfor bearing in contacts:\n    if bearing == 0:\n        print(f"HEAD-ON TRAFFIC at {bearing}° — BREAK RIGHT")\n        break\n    print(f"Traffic at {bearing}° — no conflict")\n\nprint("Scan complete")\n',
    tests: [
      { description: 'Conflict detected', expectedOutput: 'HEAD-ON TRAFFIC at 0°' },
      { description: 'Scan ends after break', expectedOutput: 'Scan complete' },
    ],
    hint: 'if bearing == 0: then break. The break exits the for loop immediately.',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 26,
    regionId: 3,
    regionStage: 6,
    title: 'Holding Pattern',
    subtitle: 'continue',
    briefing: 'Holding at FL120 — you skip certain altitudes due to traffic separation. continue skips the rest of the current loop iteration and jumps to the next one.',
    concept: 'continue',
    lesson: '`for item in list:\n    if skip_condition:\n        continue  # skip to next iteration\n    print(item)  # only runs if not skipped`\n\n`for alt in range(10000, 15001, 1000):\n    if alt == 12000:  # occupied\n        continue      # skip this altitude\n    print(f"FL{alt//100} available")`',
    starterCode: 'occupied_levels = [110, 130, 150]\n\nprint("Available holding levels:")\nfor fl in range(100, 200, 10):\n    if fl ___ occupied_levels:\n        print(f"FL{fl:03d} — OCCUPIED")\n        ___  # skip to next\n    print(f"FL{fl:03d} — available")\n',
    solution: 'occupied_levels = [110, 130, 150]\n\nprint("Available holding levels:")\nfor fl in range(100, 200, 10):\n    if fl in occupied_levels:\n        print(f"FL{fl:03d} — OCCUPIED")\n        continue\n    print(f"FL{fl:03d} — available")\n',
    tests: [
      { description: 'Occupied levels shown', expectedOutput: 'FL110 — OCCUPIED' },
      { description: 'Available levels shown', expectedOutput: 'FL100 — available' },
    ],
    hint: 'Use \'in\' to check if fl is in occupied_levels, then continue to skip to next iteration.',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 27,
    regionId: 3,
    regionStage: 7,
    title: 'Route Builder',
    subtitle: 'while + accumulator',
    briefing: "You're building a flight route string by adding waypoints one at a time until you've covered the required distance. This is the classic accumulator pattern — collecting results as you loop.",
    concept: 'while loop with accumulator, string building',
    lesson: 'Accumulate results in a variable:\n\n`total = 0\nfor n in [10, 20, 30]:\n    total += n  # accumulate\nprint(total)  # 60`\n\nBuild strings the same way:\n\n`result = ""\nfor word in words:\n    result += word + " "`',
    starterCode: 'waypoints = [\n    ("SINAI", 45), ("BEVOL", 120), ("PASOV", 200),\n    ("ALSEM", 310), ("MAXIM", 430), ("DXB", 540)\n]\ntarget_nm = 400\n\nroute = ""\ntotal_nm = 0\nwp_count = 0\n\nfor wp, dist in waypoints:\n    if total_nm ___ target_nm:\n        break\n    route += wp + " → "\n    total_nm = ___\n    wp_count += 1\n\nroute = route.rstrip(" → ")  # remove trailing arrow\nprint(f"Route: {route}")\nprint(f"Waypoints: {wp_count}, Distance: {total_nm}nm")\n',
    solution: 'waypoints = [\n    ("SINAI", 45), ("BEVOL", 120), ("PASOV", 200),\n    ("ALSEM", 310), ("MAXIM", 430), ("DXB", 540)\n]\ntarget_nm = 400\n\nroute = ""\ntotal_nm = 0\nwp_count = 0\n\nfor wp, dist in waypoints:\n    if total_nm >= target_nm:\n        break\n    route += wp + " → "\n    total_nm = dist\n    wp_count += 1\n\nroute = route.rstrip(" → ")\nprint(f"Route: {route}")\nprint(f"Waypoints: {wp_count}, Distance: {total_nm}nm")\n',
    tests: [
      { description: 'Route built', expectedOutput: 'Route: SINAI' },
      { description: 'Stats shown', expectedOutput: 'Waypoints:' },
    ],
    hint: "total_nm = dist (update to current waypoint's distance). Stop when total_nm >= target_nm.",
    xp: 100,
    isMilestone: false,
  },
  {
    id: 28,
    regionId: 3,
    regionStage: 8,
    title: 'Crew Roster',
    subtitle: 'Nested for loops',
    briefing: "You're printing the crew roster — for each flight, list all crew members. This is a nested loop: an outer loop for flights, an inner loop for crew.",
    concept: 'nested for loops',
    lesson: 'Loops inside loops:\n\n`flights = ["SQ321", "SQ322"]\ncrew = ["Captain", "FO", "FA1"]\nfor flight in flights:\n    for role in crew:\n        print(f"{flight}: {role}")`\n\nThe inner loop runs completely for each iteration of the outer loop.',
    starterCode: 'sectors = ["SIN-DXB", "DXB-LHR"]\ncrew_positions = ["Capt", "FO", "Senior FA", "FA2", "FA3"]\n\nfor sector in sectors:\n    print(f"\\n=== {sector} ===")\n    for position in ___:\n        print(f"  {position}: assigned")\n\nprint(f"\\nTotal crew slots: {len(sectors) * len(crew_positions)}")\n',
    solution: 'sectors = ["SIN-DXB", "DXB-LHR"]\ncrew_positions = ["Capt", "FO", "Senior FA", "FA2", "FA3"]\n\nfor sector in sectors:\n    print(f"\\n=== {sector} ===")\n    for position in crew_positions:\n        print(f"  {position}: assigned")\n\nprint(f"\\nTotal crew slots: {len(sectors) * len(crew_positions)}")\n',
    tests: [
      { description: 'First sector header', expectedOutput: '=== SIN-DXB ===' },
      { description: 'Total slots', expectedOutput: 'Total crew slots: 10' },
    ],
    hint: 'Inner loop: for position in crew_positions:',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 29,
    regionId: 3,
    regionStage: 9,
    title: 'Descent Profile',
    subtitle: 'loop + formatted output, zip()',
    briefing: 'The FMS prints a descent profile — altitude, speed, and distance for each fix. You need to align columns neatly using Python\'s string formatting. zip() lets you loop two lists in parallel.',
    concept: 'loop with complex formatting, zip()',
    lesson: '`zip(list1, list2)` pairs them up:\n\n`alts = [35000, 24000, 10000]\nspeeds = [480, 350, 250]\nfor alt, spd in zip(alts, speeds):\n    print(f"{alt}ft at {spd}kt")`\n\nFormat column widths with f-string format spec:\n`f"{value:<10}"` — left-align in 10 chars\n`f"{value:>8}"` — right-align in 8 chars',
    starterCode: 'fixes = ["TOD", "MAXIM", "BEVOL", "SINAI", "CH"]\naltitudes = [35000, 24000, 15000, 8000, 0]\nspeeds_kt = [480, 380, 280, 210, 0]\ndist_to_go = [250, 180, 120, 50, 0]\n\nprint(f"{\'FIX\':<8} {\'ALT\':>8} {\'SPD\':>6} {\'DTG\':>6}")\nprint("-" * 32)\n\nfor fix, alt, spd, dtg in zip(___, ___, ___, ___):\n    print(f"{fix:<8} {alt:>8}ft {spd:>5}kt {dtg:>5}nm")\n',
    solution: 'fixes = ["TOD", "MAXIM", "BEVOL", "SINAI", "CH"]\naltitudes = [35000, 24000, 15000, 8000, 0]\nspeeds_kt = [480, 380, 280, 210, 0]\ndist_to_go = [250, 180, 120, 50, 0]\n\nprint(f"{\'FIX\':<8} {\'ALT\':>8} {\'SPD\':>6} {\'DTG\':>6}")\nprint("-" * 32)\n\nfor fix, alt, spd, dtg in zip(fixes, altitudes, speeds_kt, dist_to_go):\n    print(f"{fix:<8} {alt:>8}ft {spd:>5}kt {dtg:>5}nm")\n',
    tests: [
      { description: 'Header shown', expectedOutput: 'FIX' },
      { description: 'TOD line shown', expectedOutput: 'TOD' },
    ],
    hint: 'zip(fixes, altitudes, speeds_kt, dist_to_go) — all four lists together',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 30,
    regionId: 3,
    regionStage: 10,
    title: 'Commercial Check',
    subtitle: 'Autopilot Milestone',
    briefing: "Commercial rating check! You've mastered loops. This final loop challenge combines everything: iterate, accumulate, filter, format, and build a complete flight log from raw data.",
    concept: 'Review: for, while, range, enumerate, break, continue, zip',
    lesson: 'Review:\n- `for x in list:` — iterate a list\n- `for i, x in enumerate(list):` — with index\n- `for a, b in zip(l1, l2):` — parallel lists\n- `range(start, stop, step)` — number sequences\n- `while condition:` — conditional loop\n- `break` — exit early, `continue` — skip iteration',
    starterCode: 'flights = [\n    {"id": "SQ321", "dist": 6763, "pax": 287, "status": "complete"},\n    {"id": "SQ317", "dist": 3365, "pax": 312, "status": "complete"},\n    {"id": "SQ802", "dist": 890,  "pax": 0,   "status": "cancelled"},\n    {"id": "SQ221", "dist": 9534, "pax": 301, "status": "complete"},\n]\n\ntotal_dist = 0\ntotal_pax = 0\n\nprint(f"{\'FLIGHT\':<8} {\'DIST\':>7} {\'PAX\':>5} {\'STATUS\':<12}")\nprint("-" * 38)\n\nfor i, f in enumerate(flights, 1):\n    if f["status"] == "cancelled":\n        print(f"{f[\'id\']:<8} {\'CANCELLED\':>7}")\n        ___  # skip accumulation\n    print(f"{i}. {f[\'id\']:<6} {f[\'dist\']:>7}nm {f[\'pax\']:>5} {f[\'status\']:<12}")\n    total_dist ___= f["dist"]\n    total_pax ___= f["pax"]\n\nprint(f"\\nTotal: {total_dist}nm flown, {total_pax} passengers carried")\n',
    solution: 'flights = [\n    {"id": "SQ321", "dist": 6763, "pax": 287, "status": "complete"},\n    {"id": "SQ317", "dist": 3365, "pax": 312, "status": "complete"},\n    {"id": "SQ802", "dist": 890,  "pax": 0,   "status": "cancelled"},\n    {"id": "SQ221", "dist": 9534, "pax": 301, "status": "complete"},\n]\n\ntotal_dist = 0\ntotal_pax = 0\n\nprint(f"{\'FLIGHT\':<8} {\'DIST\':>7} {\'PAX\':>5} {\'STATUS\':<12}")\nprint("-" * 38)\n\nfor i, f in enumerate(flights, 1):\n    if f["status"] == "cancelled":\n        print(f"{f[\'id\']:<8} {\'CANCELLED\':>7}")\n        continue\n    print(f"{i}. {f[\'id\']:<6} {f[\'dist\']:>7}nm {f[\'pax\']:>5} {f[\'status\']:<12}")\n    total_dist += f["dist"]\n    total_pax += f["pax"]\n\nprint(f"\\nTotal: {total_dist}nm flown, {total_pax} passengers carried")\n',
    tests: [
      { description: 'Cancelled shown', expectedOutput: 'CANCELLED' },
      { description: 'Total distance', expectedOutput: 'Total:' },
    ],
    hint: 'Use continue to skip cancelled flights. += to accumulate totals.',
    xp: 500,
    isMilestone: true,
  },

  // ─────────────────────────────────────────────
  // REGION 4: FMS Programming — Functions (pt 1)
  // ─────────────────────────────────────────────
  {
    id: 31,
    regionId: 4,
    regionStage: 1,
    title: 'Distance Function',
    subtitle: 'def and return',
    briefing: 'You write the same distance calculation in three different places. Time to make it a function. Functions are reusable blocks of code you define once and call many times.',
    concept: 'def, return',
    lesson: '`def function_name(parameters):\n    # code\n    return result`\n\n`def nm_to_km(nm):\n    return nm * 1.852\n\nprint(nm_to_km(3365))  # 6232.58`\n\nThe `return` statement sends a value back to the caller.',
    starterCode: '# Define a function to convert nautical miles to km\n___ nm_to_km(nm):\n    ___ nm * 1.852\n\n# Define a function to calculate flight time in hours\n___ flight_time(distance_nm, speed_kts):\n    ___ distance_nm / speed_kts\n\n# Use them\nroutes = [("SIN-LHR", 6763), ("SIN-SYD", 3907), ("SIN-JFK", 9534)]\nfor name, dist in routes:\n    km = nm_to_km(dist)\n    hrs = flight_time(dist, 480)\n    print(f"{name}: {round(km)}km, {round(hrs, 1)}h")\n',
    solution: 'def nm_to_km(nm):\n    return nm * 1.852\n\ndef flight_time(distance_nm, speed_kts):\n    return distance_nm / speed_kts\n\nroutes = [("SIN-LHR", 6763), ("SIN-SYD", 3907), ("SIN-JFK", 9534)]\nfor name, dist in routes:\n    km = nm_to_km(dist)\n    hrs = flight_time(dist, 480)\n    print(f"{name}: {round(km)}km, {round(hrs, 1)}h")\n',
    tests: [
      { description: 'SIN-LHR shown', expectedOutput: 'SIN-LHR:' },
      { description: 'SIN-JFK shown', expectedOutput: 'SIN-JFK:' },
    ],
    hint: 'def function_name(param): then return value. Use nm * 1.852 for nm_to_km.',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 32,
    regionId: 4,
    regionStage: 2,
    title: 'Fuel Calculator Function',
    subtitle: 'Multiple parameters & default values',
    briefing: 'The fuel calculator needs distance, speed, and burn rate. Functions can take multiple parameters, and you can give them default values so callers don\'t always have to provide every argument.',
    concept: 'multiple parameters, default values',
    lesson: '`def greet(name, greeting="Hello"):\n    return f"{greeting}, {name}!"\n\ngreet("Tower")           # "Hello, Tower!"\ngreet("Tower", "Good evening")  # "Good evening, Tower!"`\n\nDefault values let the caller omit optional arguments.',
    starterCode: 'def fuel_required(distance_nm, speed_kts=480, burn_rate=6800, reserve_pct=10):\n    # Calculate flight time\n    time_h = ___\n    # Trip fuel\n    trip_fuel = ___\n    # Reserve fuel\n    reserve = ___\n    # Total\n    total = trip_fuel + reserve\n    return round(total)\n\n# LHR with all defaults\nprint(f"SIN-LHR: {fuel_required(6763)} kg")\n\n# SYD with slower speed\nprint(f"SIN-SYD slow: {fuel_required(3907, speed_kts=430)} kg")\n\n# JFK with higher burn (headwind) and 15% reserve\nprint(f"SIN-JFK heavy: {fuel_required(9534, burn_rate=7200, reserve_pct=15)} kg")\n',
    solution: 'def fuel_required(distance_nm, speed_kts=480, burn_rate=6800, reserve_pct=10):\n    time_h = distance_nm / speed_kts\n    trip_fuel = burn_rate * time_h\n    reserve = trip_fuel * (reserve_pct / 100)\n    total = trip_fuel + reserve\n    return round(total)\n\nprint(f"SIN-LHR: {fuel_required(6763)} kg")\nprint(f"SIN-SYD slow: {fuel_required(3907, speed_kts=430)} kg")\nprint(f"SIN-JFK heavy: {fuel_required(9534, burn_rate=7200, reserve_pct=15)} kg")\n',
    tests: [
      { description: 'LHR fuel shown', expectedOutput: 'SIN-LHR:' },
      { description: 'JFK fuel shown', expectedOutput: 'SIN-JFK heavy:' },
    ],
    hint: 'time_h = distance_nm / speed_kts. trip_fuel = burn_rate * time_h. reserve = trip_fuel * (reserve_pct/100)',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 33,
    regionId: 4,
    regionStage: 3,
    title: 'Wind Components',
    subtitle: 'Multiple return values (tuples)',
    briefing: 'Wind components split a wind into headwind/tailwind and crosswind. A Python function can return two values at once — Python packs them as a tuple, and you can unpack them with two variable names.',
    concept: 'returning multiple values, tuple unpacking',
    lesson: '`def min_max(lst):\n    return min(lst), max(lst)  # returns a tuple\n\nlo, hi = min_max([3, 1, 4, 1, 5])  # unpack\nprint(lo, hi)  # 1 5`\n\nYou can also return explicitly as a tuple:\n`return (value1, value2)`',
    starterCode: 'import math\n\ndef wind_components(wind_speed, wind_dir, runway_hdg):\n    """Returns (headwind, crosswind) in knots.\n    Positive headwind = into the wind. Positive crosswind = from right."""\n    angle = math.radians(wind_dir - runway_hdg)\n    headwind = wind_speed * math.cos(angle)\n    crosswind = wind_speed * math.sin(angle)\n    return round(headwind, 1), round(crosswind, 1)\n\n# Runway 20R = 200° heading\nhdw, xw = wind_components(___, ___, 200)\nprint(f"Wind 15kt at 180°:")\nprint(f"  Headwind: {hdw}kt")\nprint(f"  Crosswind: {xw}kt")\n\n# Try runway 02L = 020° heading\nhdw2, xw2 = wind_components(20, 280, 20)\nprint(f"Wind 20kt at 280° on 02L:")\nprint(f"  Headwind: {hdw2}kt, Crosswind: {xw2}kt")\n',
    solution: 'import math\n\ndef wind_components(wind_speed, wind_dir, runway_hdg):\n    angle = math.radians(wind_dir - runway_hdg)\n    headwind = wind_speed * math.cos(angle)\n    crosswind = wind_speed * math.sin(angle)\n    return round(headwind, 1), round(crosswind, 1)\n\nhdw, xw = wind_components(15, 180, 200)\nprint(f"Wind 15kt at 180°:")\nprint(f"  Headwind: {hdw}kt")\nprint(f"  Crosswind: {xw}kt")\n\nhdw2, xw2 = wind_components(20, 280, 20)\nprint(f"Wind 20kt at 280° on 02L:")\nprint(f"  Headwind: {hdw2}kt, Crosswind: {xw2}kt")\n',
    tests: [
      { description: 'Headwind shown', expectedOutput: 'Headwind:' },
      { description: 'Crosswind shown', expectedOutput: 'Crosswind:' },
    ],
    hint: 'wind_components(15, 180, 200) — speed=15, wind direction=180°, runway heading=200°',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 34,
    regionId: 4,
    regionStage: 4,
    title: 'Recursive Descent',
    subtitle: 'Recursion basics',
    briefing: 'The FMS calculates step-climbs recursively — each altitude step calls the same calculation for the next step. Recursion is when a function calls itself. It must have a base case to stop.',
    concept: 'recursion, base case',
    lesson: '`def countdown(n):\n    if n <= 0:       # base case — STOP\n        print("Liftoff!")\n        return\n    print(n)\n    countdown(n - 1)  # calls itself with smaller n`\n\nWithout a base case, recursion runs forever (RecursionError).',
    starterCode: 'def step_climb(current_fl, target_fl, step=20):\n    """Print each step climb from current_fl to target_fl."""\n    # Base case: reached or passed target\n    if current_fl ___ target_fl:\n        print(f"FL{target_fl:03d} — Cruise altitude reached")\n        return\n    \n    print(f"FL{current_fl:03d} — climbing...")\n    # Recursive call: next step\n    step_climb(current_fl + ___, target_fl, step)\n\n# Climb from FL280 to FL390 in FL20 steps\nstep_climb(280, 390)\n',
    solution: 'def step_climb(current_fl, target_fl, step=20):\n    if current_fl >= target_fl:\n        print(f"FL{target_fl:03d} — Cruise altitude reached")\n        return\n    \n    print(f"FL{current_fl:03d} — climbing...")\n    step_climb(current_fl + step, target_fl, step)\n\nstep_climb(280, 390)\n',
    tests: [
      { description: 'Climb started', expectedOutput: 'FL280 — climbing...' },
      { description: 'Cruise reached', expectedOutput: 'FL390 — Cruise altitude reached' },
    ],
    hint: 'Base case: if current_fl >= target_fl. Recursive call adds step: step_climb(current_fl + step, ...)',
    xp: 100,
    isMilestone: false,
  },
  {
    id: 35,
    regionId: 4,
    regionStage: 5,
    title: 'Scope Check',
    subtitle: 'Local vs global scope',
    briefing: 'The flight computer has a global fuel register and local calculation variables. In Python, variables created inside functions are local — they cannot be seen outside. The global keyword lets a function modify a global variable.',
    concept: 'local scope, global scope, global keyword',
    lesson: '`fuel = 45000  # global\n\ndef burn(amount):\n    local_calc = amount * 1.05  # local only\n    global fuel\n    fuel -= amount  # modifies global\n    return local_calc\n\nburn(1000)\nprint(fuel)  # 44000`\n\nGenerally, prefer returning values over modifying globals — but knowing scope is essential.',
    starterCode: 'total_fuel_kg = 45200  # global\nflight_hours = 0        # global\n\ndef fly_sector(distance_nm, speed_kts=480, burn_rate=6800):\n    # Declare we\'re modifying globals\n    ___ total_fuel_kg\n    ___ flight_hours\n    \n    hours = distance_nm / speed_kts\n    burned = burn_rate * hours\n    \n    # Update globals\n    total_fuel_kg -= burned\n    flight_hours += hours\n    \n    return round(burned)\n\n# Fly three sectors\nfor sector, dist in [("SIN-DXB", 3379), ("DXB-FCO", 2718), ("FCO-LHR", 895)]:\n    burned = fly_sector(dist)\n    print(f"{sector}: burned {burned:.0f}kg, remaining {total_fuel_kg:.0f}kg")\n\nprint(f"\\nTotal flight time: {flight_hours:.1f}h")\n',
    solution: 'total_fuel_kg = 45200\nflight_hours = 0\n\ndef fly_sector(distance_nm, speed_kts=480, burn_rate=6800):\n    global total_fuel_kg\n    global flight_hours\n    \n    hours = distance_nm / speed_kts\n    burned = burn_rate * hours\n    \n    total_fuel_kg -= burned\n    flight_hours += hours\n    \n    return round(burned)\n\nfor sector, dist in [("SIN-DXB", 3379), ("DXB-FCO", 2718), ("FCO-LHR", 895)]:\n    burned = fly_sector(dist)\n    print(f"{sector}: burned {burned:.0f}kg, remaining {total_fuel_kg:.0f}kg")\n\nprint(f"\\nTotal flight time: {flight_hours:.1f}h")\n',
    tests: [
      { description: 'SIN-DXB shown', expectedOutput: 'SIN-DXB:' },
      { description: 'Total flight time', expectedOutput: 'Total flight time:' },
    ],
    hint: "Use 'global total_fuel_kg' and 'global flight_hours' at the top of the function.",
    xp: 100,
    isMilestone: false,
  },
];
